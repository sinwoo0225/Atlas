# 2026-05-15 (3)
## 메뉴 : 데스크톱·웹서비스·앱호스트 — WebView2 인프로세스 호스팅 전환
### 요청원문 : 지금 구조에서 혹시 백엔드 서버 호스팅 없이 단일 스탠드얼론 앱으로 동작하려면 얼마나 바꿔야할까? 포트 오픈 등의 보안 정책 위반 리스크를 대비해서.

- [완료] 리팩토링 | AppHost 라이브러리 분리 | 컨트롤러 10개 + DI 와이어링을 `ProjectManager.WebService` → 새 `ProjectManager.AppHost` 로 이동. `Microsoft.NET.Sdk` + `<FrameworkReference Include="Microsoft.AspNetCore.App" />`. `Program.cs` 의 등록 로직을 `AppHostFactory.Build(WebApplicationBuilder)` 정적 메서드로 추출 — `ATLAS_INPROC=1` 환경변수일 때만 정적파일 미들웨어 skip. WebService 는 AppHost 만 참조하는 4 줄짜리 `Program.cs` 로 축소 — dev `dotnet run --project ProjectManager.WebService` + EF migrations tooling 흐름 그대로 유지. Core/Application/Infrastructure/frontend/`start.ps1` 무변경
- [완료] 신기능 | DesktopApp 인프로세스 호스팅 | `InProcessHost.cs` 신설 — `Microsoft.AspNetCore.TestHost.TestServer` 위에 `AppHostFactory.Build` 한 후 `_app.StartAsync()` + `GetTestServer().CreateClient()`. Kestrel 미부팅이므로 listen 소켓 없음. `MainWindow.xaml.cs` 의 spawn 흐름(`Process.Start`/`WaitForBackendAsync`/`Kill`) 전부 제거 → `OnLoaded` 에서 `InProcessHost.StartAsync` → WebView2 초기화 → `AddWebResourceRequestedFilter` + `WebResourceRequested` 핸들러 → `Navigate(https://atlas.local/index.html)`. `OnClosing` 에서 `_host.DisposeAsync`
- [완료] 버그수정 | `SetVirtualHostNameToFolderMapping` + `WebResourceRequested` 공존 함정 | 같은 호스트에 두 가지를 동시 설정하면 가상호스트가 우선 처리되어 핸들러 발화 자체가 차단 → `/api/*` fetch 가 정적파일 lookup 으로 가서 404 → 사용자 UI 에 "프로젝트 목록을 불러올 수 없다" 표시. 가상호스트 제거하고 핸들러 하나로 통일: `/api/*` 는 in-process ASP.NET 으로 프록시, 그 외는 `BuildStaticResponse` 가 `wwwroot/` 에서 직접 read + `MimeMap` 으로 Content-Type 결정 + 파일 부재 시 `index.html` 로 SPA fallback(React Router 지원) + 디렉토리 탈출 방지(`Path.GetFullPath` 가 root prefix 로 시작하는지 검증)
- [완료] 신기능 | multipart/스트리밍 통과 핸들러 | `OnApiRequested`: 본문을 `byte[]` 로 한 번 buffer 후 `ByteArrayContent` 로 송신 — `Content-Type` 은 일반 헤더 분리 처리(`req.Headers.TryAddWithoutValidation` false 면 content 헤더로) 가 아니라 별도 추출해 `MediaTypeHeaderValue.TryParse` 로 `content.Headers.ContentType` 에 명시 설정. multipart 의 `boundary` 가 든 `Content-Type` 이 정확히 들어가야 `IFormFile` 파싱이 작동. `AppHostFactory` 에서 `FormOptions.MultipartBodyLengthLimit / ValueLengthLimit / MultipartHeadersLengthLimit` 모두 무제한
- [완료] 개선 | `App.xaml.cs` 의 `Application` 이름 충돌 | DesktopApp 이 AppHost 통해 `ProjectManager.Application` 네임스페이스를 transitive 노출하면서 `partial class App : Application` 의 base 가 모호해짐. `: System.Windows.Application` 으로 fully-qualified 변경
- [완료] 빌드 | DesktopApp 단일 산출물 + PublishFrontend target 이전 | DesktopApp.csproj Release 모드 PropertyGroup 에 `PublishSingleFile=true` / `SelfContained=true` / `RuntimeIdentifier=win-x64` / `IncludeNativeLibrariesForSelfExtract=true` / `EnableCompressionInSingleFile=true` / `PublishDir=publish/`. WebService.csproj 의 `PublishFrontend` MSBuild Target 을 DesktopApp.csproj 로 이전(`AfterTargets="Publish"`, 출력 `$(PublishDir)wwwroot\`). publish.ps1 단순화 — WebService publish 호출과 `ProjectManager.WebService.exe` 결과 확인 제거. `publish/Atlas.exe`(82MB single-file) + `publish/wwwroot/` 만 산출 — 별도 백엔드 exe 부재 확인
- [완료] 개선 | 진단 로그(에러 전용) | `Documents/ProjectManager/atlas-debug.log` 에 핸들러 catch 블록 / 4xx5xx 응답(status + body preview 400B) / 정적파일 read 실패만 한 줄씩 기록. 정상 경로는 무로깅이라 평소엔 파일 거의 안 자람
- [완료] 빌드 | 솔루션 빌드 + 회귀 확인 | `dotnet build ProjectManager.slnx` 0 warning/0 error. dev `dotnet run --project ProjectManager.WebService` 가 5200 에서 정상(`/api/health`/`/api/projects`/`/api/monitoring/today` 모두 200 OK — AppHost 분리에도 dev 무회귀). publish 산출물에서 GET/PUT/POST/DELETE 사용자 검증 통과 — DELETE 가 204 No Content + 후속 GET 응답 크기 감소로 실제 삭제 확인

### 함정 메모

- **WebView2 가상호스트와 WebResourceRequested 가 같은 호스트에 공존 불가**. `SetVirtualHostNameToFolderMapping` 으로 매핑된 호스트는 가상호스트가 처리를 가로채서 `WebResourceRequested` 가 발화하지 않는다. API 프록시가 필요하면 둘 중 하나만 쓰고 정적파일도 같은 핸들러로 처리해야 한다.
- **DesktopApp(`Microsoft.NET.Sdk`) 이 WebService(`Microsoft.NET.Sdk.Web`) 를 직접 ProjectReference 하면 publish 시 wwwroot/appsettings 가 SDK 충돌**. 그래서 컨트롤러+DI 를 일반 SDK 라이브러리 `ProjectManager.AppHost` 로 분리해 양쪽이 안전하게 참조하게 했다.
- **AppHost 의 `AddControllers` 다음에 `AddApplicationPart(typeof(AppHostFactory).Assembly)` 필수**. DesktopApp 의 entry assembly 에는 컨트롤러가 없어 명시하지 않으면 컨트롤러 디스커버리가 실패한다.
- **`AppHost`(`Microsoft.NET.Sdk`) 와 `DesktopApp`(WPF) 모두 `ImplicitUsings=enable` 이지만 Web SDK 가 아니라서 `Microsoft.AspNetCore.Http` 가 implicit 에 포함 안 됨**. `IFormFile` 같은 타입은 명시적 `using` 필요.
- **`PublishFrontend` MSBuild Target 의 `BeforeTargets="BeforePublish"` 는 Web SDK 에서만 신뢰성 있게 발화**. 일반 SDK + WPF 에서는 `AfterTargets="Publish"` 가 안전.
- **single-file publish 에서 `IncludeAllContentForSelfExtract=true` 켜면 `wwwroot/` 가 임시 추출 폴더로 묶임** — 외부 폴더로 두려면 켜지 말 것. 우리도 `Environment.ProcessPath` 의 디렉토리 옆에 `wwwroot/` 가 있어야 가상호스트 없이 핸들러가 직접 read 가능.

---

# 2026-05-15 (2)
## 메뉴 : 일정/WBS — 간트차트 후속 픽스 2차 3건
### 요청원문 : 드래그 할 때 다시 바가 겹치는 위치에 포인터가 존재하면 클릭 이벤트가 다시 발생하는 문제가 있어. 그리고 차트에 호버 시 나오는 툴팁에 날짜가 실제 데이터와 1일씩 차이가 나는 것 같고. 그리고 바의 양 끝에 도형 표시는 여전히 안나와.

- [완료] 버그수정 | 1. 툴팁 날짜 1일 차이 (KST 시간대) | `toIsoDate` 가 `Date.toISOString().slice(0,10)` 로 UTC 변환했던 게 원인. KST(UTC+9) 로컬 자정(`2026-05-15T00:00:00+09:00`) → UTC 변환 시 `2026-05-14T15:00:00Z` → slice 결과 `2026-05-14` 로 전날 표시. 폼 `<input type="date">` 값(YYYY-MM-DD 로컬 자정) 과 1:1 매칭되려면 로컬 시간 기준 포매팅 필요 → `getFullYear/getMonth/getDate` 로 직접 조립. `snapToDay` 도 UTC ms 반올림 → 로컬 자정 기준 반올림으로 변경(자정+다음자정 중간점 비교)
- [완료] 버그수정 | 2. 드래그 중 막대 위 mousedown 재트리거 | `chart.on('mousedown')` 에 진입 시 `dragRef.current` 가 있으면 early return. 같은 의미로 `chart.on('mouseover/mouseout')` 도 드래그 중에는 hoveredId 갱신 skip — drag 중 hover 트리거가 React re-render → 옵션 patch → 좌표 흔들림으로 이어지는 경로 차단
- [완료] 신기능 | 3. 호버 핸들 graphic API 로 분리 | 기존: `seriesData.value` 에 hoverFlag 차원 추가 → renderItem 안에서 hover 시 inline 핸들 도형 그리기. 문제: ECharts `mouseover` 이벤트가 차트 재구성 후 안정적으로 안 트리거됨 → 핸들 안 보임. 픽스: ①seriesData 에서 hover 차원 제거, renderItem 의 핸들 분기 제거 ②`showHandles(yIdx, row)` / `hideHandles()` 함수 추가 — ECharts graphic API + `replaceMerge: 'graphic'` 로 4×20 둥근 사각형(액센트색·테두리 1px) 좌·우 핸들 직접 패치 ③차트 wrapper 의 onMouseMove 에서 DOM hit-test 후 직접 호출(ECharts hover 이벤트 의존 X) ④`lastHoverRowRef` 캐시로 같은 row 면 패치 skip ⑤드래그 중에는 graphic 슬롯이 미리보기에 점유돼야 하므로 hideHandles 가 dragRef 가드
- [완료] 버그수정 | ReactECharts notMerge 제거 | 핸들·드래그 미리보기는 `chart.setOption({graphic:[...]}, {replaceMerge:'graphic'})` 로 ECharts 인스턴스에 직접 패치하는데, ReactECharts 의 setOption 이 `notMerge=true` 모드면 React re-render 마다 graphic 옵션이 비어있다고 보고 전체 교체 → 우리가 패치한 graphic 이 즉시 사라짐. `notMerge` 제거(기본 false 머지 모드) → 옵션의 graphic 키가 없으면 ECharts 가 손대지 않음. dataZoom 의 zoomRange 명시 보존은 그대로 유지
- [완료] 빌드 | npm run build → 통과 (gzip 695KB, 변화 없음)
- [완료] 빌드 | npm run lint → GanttChart 에러 0

---

# 2026-05-15
## 메뉴 : 일정/WBS — 간트차트 후속 픽스 4건
### 요청원문 : 간트 차트 드래그로 날짜 조정은 이상해, 일단 끝 부분 조정 가능한 영역은 확장 가능함을 시각적으로 알 수 있게 화살표 같은게 호버일 때 나와야 할 것 같구, 누른채로 드래그 하면 날짜가 제대로 안착되지가 않아 이벤트 문제같아. 그리고 아래 시간 간격 줌 해서 보다가 바 위에 마우스 올리면 다시 최소화돼. 그리고 내보내기는 차트는 잘 나오는데 왼쪽 작업 이름 등이 나오지가 않아서 효용이 적어. 개선 플랜 세우자

- [완료] 신기능 | 1. 드래그 핸들 + 동적 cursor | renderItem 의 막대 분기에 hoverFlag 차원 추가 — `hoveredId === itemId` 일 때만 좌·우 작은 핸들 도형(width 3px, height 12px, 다크/라이트별 반투명 색) 표시. 차트 영역 wrapper 에 onMouseMove 추가 → chart.convertFromPixel 로 (시간, yIdx) 환산 → 막대 hit-test → 좌·우 6px=ew-resize / 중앙=grab / 막대 밖=default 로 chartAreaRef.style.cursor 직접 변경. lastCursorRef 캐시로 변동 없으면 setState 안 함
- [완료] 버그수정 | 2. 드래그 날짜 안착 문제 | 핵심 원인은 매 mousemove 마다 `setDragPreview` → React re-render → `notMerge=true` 옵션 patch → 차트 전체 재구성 → dataZoom 리셋 + 좌표 변환 캐시 흔들림 + 누적 오차. 해결: ①dragPreview state 제거. ②미리보기는 ECharts `graphic` API + `replaceMerge: 'graphic'` 으로 점선 stroke rect 만 패치(series/dataZoom 영향 0). ③`msPerPx` 를 mousedown 시점에 1회 계산해 dragRef 에 저장 → mousemove 중 변환 비율 고정. ④window mousemove/mouseup 리스너를 mount 시 1회만 등록(deps=[]), 핸들러는 dragRef.current 만 참조. ⑤최신 rows·projectId·onItemsChanged 는 ref 로 보관(useEffect 안에서만 sync)
- [완료] 버그수정 | 3. 호버·필터·스케일 변경 시 dataZoom 리셋 | 원인: dataZoom 옵션에 start/end 가 안 적혀있어 옵션 patch 시 기본값(0~100) 으로 복원되던 문제. 해결: `zoomRange: {start, end}` state 추가, chart.on('datazoom') 으로 현재 dataZoom 의 start/end 백분율 캐치 → setZoomRange. 옵션 빌드 시 dataZoom 두 항목 모두에 start/end 명시. 결과: notMerge=true 라도 매 옵션 patch 마다 사용자 zoom 위치 그대로 복원
- [완료] 신기능 | 4. PNG 저장에 좌측 라벨 포함 | toolbox.saveAsImage 제거(ECharts canvas 만 캡처해서 HTML 좌측 라벨 누락). `html-to-image` 패키지 추가, 상단 툴바 우측에 [PNG 저장] 버튼(Lucide Download). 클릭 시 setHoveredId(null) → requestAnimationFrame 대기 → htmlToImage.toPng(wrapperRef.current, {backgroundColor, pixelRatio:2, cacheBust:true}) → `<a download>` 트리거. wrapper div(좌측 LabelColumn + ECharts) 통째로 캡처되어 들여쓰기·Chevron·마일스톤 아이콘까지 그대로 포함
- [완료] 빌드 | npm install html-to-image (1 package added, 0 vulnerabilities)
- [완료] 빌드 | cd frontend; npm run build → tsc + vite 통과 (gzip 690→695KB, html-to-image 약 5KB 증가)
- [완료] 빌드 | cd frontend; npm run lint → GanttChart 에러 0 (ref mutation 은 useEffect 로 분리해 react-hooks/purity 회피)

---

# 2026-05-14 (3)
## 메뉴 : 일정/WBS — 간트차트 강화
### 요청원문 : 간트차트 부분을 강화하고 싶어. 지금은 기능이 너무 없고, 확장/접기 기능도 없어서 보기도 불편해. 정렬도 이상한 것 같고.

- [완료] 버그수정 | 정렬 트리 깨짐 | 기존 GanttChart 가 flattenItems 후 startDate 로 전체 재정렬해 부모-자식이 화면에서 흩어지고 사용자가 폼에서 정한 order(중요도) 가 무시되던 문제. `flattenForGantt(items, collapsed)` 를 DFS pre-order + 같은 형제 그룹 내에서만 order desc → startDate asc 보조 정렬로 재작성. 트리 구조가 화면에 그대로 보존됨
- [완료] 신기능 | 확장/접기 + 부모 막대 자동 계산 | 좌측 라벨의 Chevron 으로 부모 행 토글. `collapsed: Set<number>` state. 접힌 부모는 후손들의 min(start)~max(end) 로 자동 계산된 단일 막대. 펼친 부모는 본인/자식 합산 범위로 얇은 음영 막대로 행 위에 표시(참고용)
- [완료] 신기능 | 좌측 HTML 라벨 영역 | ECharts yAxis.show=false 로 끄고 HTML LabelColumn(width 280px, row height 32px) 로 들여쓰기·Chevron·마일스톤 다이아몬드·작업명·담당자 표시. ECharts grid top/bottom 패딩과 행 높이 동기화. 행 호버시 좌측 라벨 배경 강조
- [완료] 신기능 | 상태별 막대 색 | wbsStatusBadge 와 동일 의미 매핑. themeColors.ts ChartColors 에 ganttBarPlanned/InProgress/Done · ganttBarParent · ganttMilestone · ganttToday · ganttWeekend · ganttRowHover 추가 (라이트/다크 두 톤). renderItem 안에서 kind='bar'|'parent'|'milestone' 별로 도형 분기
- [완료] 신기능 | 마일스톤 다이아몬드 | isMilestone===true 항목은 막대 대신 EndDate(없으면 StartDate) 좌표에 단일 다이아몬드(폴리곤). renderItem 분기
- [완료] 신기능 | 오늘 표시선 + 주말 음영 | series.markLine 으로 오늘 빨강 점선 + "오늘" 라벨, series.markArea 로 토/일 음영. 데이터 범위 내 일 단위 순회로 markArea 자동 생성 (최대 730일 캡)
- [완료] 신기능 | 일/주/월 스케일 토글 + dataZoom | 상단 툴바에 [일][주][월] 버튼. xAxis.minInterval + axisLabel.formatter 동적 교체. 하단에 dataZoom inside(ctrl+wheel)+slider 추가 — 데이터 필터링은 안 함(filterMode='none')
- [완료] 신기능 | 드래그로 일정 변경 | chart.on('mousedown') 의 픽셀 위치로 막대 좌5px=resize-l / 우5px=resize-r / 중간=move 분기. window mousemove 에서 convertFromPixel 로 픽셀→ms 환산 후 일 단위 스냅. mouseup 에서 wbsApi.update 호출 + onItemsChanged 콜백으로 부모 fetch. 드래그 중 dragPreview state 로 막대 위치 실시간 반영
- [완료] 신기능 | 필터(상태·담당자) + PNG 내보내기 | 툴바 필터 토글 → 칩 그룹으로 상태(Planned/InProgress/Done)·담당자 멀티 선택. 매치 안 되는 행은 좌측 라벨/막대 모두 opacity 0.35. ECharts toolbox.saveAsImage 우측 상단 활성화 — pixelRatio 2, 테마별 배경
- [완료] 리팩토링 | 컴포넌트 분리 | WbsPage.tsx 의 GanttChart 함수 80줄을 frontend/src/pages/wbs/GanttChart.tsx 신규 파일(약 620줄) 로 분리. WbsPage 의 flattenItems 헬퍼 제거(트리 보존 정렬 함수가 대체). WbsPage 의 사용처에 projectId/onItemsChanged props 추가
- [완료] 빌드 | cd frontend; npm run build → tsc + vite 통과
- [완료] 빌드 | cd frontend; npm run lint → GanttChart 신규 파일 관련 에러 0 (Date.now() 는 `useState(() => Date.now())` lazy initializer 로 react-hooks/purity 회피, useEffect deps 의 `dragPreview != null` 은 `isDragging` 변수로 추출, ECharts 콜백의 any 룰은 파일 단위 disable — 다른 ECharts 사용처와 동일 관행)

---

# 2026-05-14 (2)
## 메뉴 : 공통(BadgeMenu/Tab들여쓰기)/회의록/이슈/변경이력/WBS/프로젝트목록/업무일지/데스크톱
### 요청원문 : 직전 10건 머지 후 실사용에서 나온 후속 버그·개선 — 로딩스피너 호 안 보임, 업무일지 카드 헤더 정렬, WBS 상태 dropdown 디자인, 이슈 인라인 편집 표+아코디언, 변경이력 메타 컴팩트, BadgeMenu 클리핑, 회의록 시간 30분, 프로젝트카드 우측 상단, 마크다운 Tab 들여쓰기

- [완료] 버그수정 | 1. 로딩 스피너 호 가시화 | MainWindow.xaml: `StrokeDashArray="35 200"` (StrokeThickness 4 배수라 둘레 거의 전부 칠해짐) → `"9.5 28"` 으로 1/4 둘레만 보이게. 회전 호가 시각적으로 인식됨
- [완료] 버그수정 | 2. 업무일지 5개 요일 카드 헤더 y 정렬 | WorkLogPage PreviewCard: `<button>` 에 `flex flex-col h-full`, 헤더 `shrink-0`, 콘텐츠 `flex-1`. grid stretch 와 함께 5개 카드 동일 높이 + 헤더 정확히 top 정렬
- [완료] 신기능 | 3. BadgeMenu 컴포넌트 | components/ui/BadgeMenu.tsx 신규: Badge 형 트리거 + 커스텀 dropdown(외부 클릭/스크롤/리사이즈 닫힘). 표 부모 `overflow-hidden` 안에서도 잘리지 않게 `createPortal` 로 body 에 mount + 트리거 getBoundingClientRect 기준 fixed positioning
- [완료] 개선 | 4. WBS 상태 셀 BadgeMenu 적용 | WbsPage.tsx: 투명 native select 오버레이 제거 → `BadgeMenu<WbsStatus>` 로 교체. 다크 톤 dropdown 옵션 노출. 기존 옵티미스틱 patchStatus + load() 그대로
- [완료] 개선 | 5. 이슈 관리 인라인 편집 표 + 아코디언 | IssuesPage.tsx 전면 재구성: 표 셀에서 제목(input)·상태/우선순위(BadgeMenu)·담당자(select)·마감일(date) 인라인 편집. 좌측 토글 chevron → 펼침 행에 description 마크다운 인플레이스 편집(DescriptionField). 표 마지막 빈 행: 제목+Enter 로 즉시 생성. 기존 IssueForm 모달·상단 등록 버튼·행 클릭 모달 패턴 제거
- [완료] 개선 | 6. 변경이력 카드 메타 컴팩트 | ChangeLogsPage.tsx: 두 vertical 섹션(라벨+컨텐츠×2, 최소 4줄) → 단일 flex-wrap 행으로 통합. 라벨 텍스트 제거하고 Paperclip(회의록)/Link(문서) 아이콘으로 구분. 문서 링크는 formatLinkLabel(URL이면 host+path 마지막 세그먼트, 평문 경로면 마지막 세그먼트)로 축약 + title 호버 풀 URL. 메타 영역 mt/pt 절반 축소
- [완료] 개선 | 7. 회의록 시간 입력 30분 단위 | MeetingsPage.tsx: 시작/종료 시간을 `<input type="time">` → `<select>` 로 교체. HALF_HOUR_SLOTS(48개) 옵션. Chromium native time picker 의 분 spinner 가 step 무시하고 0~59 다 노출하는 한계 회피. 데이터 모델은 그대로 "HH:mm"
- [완료] 버그수정 | 8. 회의록 시간 input 잘림 | MeetingsPage grid 비율: col-4/2/2/4 → col-3/5/4 (날짜/시간묶음/주제). 시간 두 input 한 셀에 inline `~` 구분자 + 각 `min-w-[7.5rem] flex-1` 로 시계 picker 아이콘이 텍스트 위에 안 겹치게
- [완료] 신기능 | 9. textareaTab 헬퍼 | utils/textareaTab.ts: applyTextareaTab(e, setValue) — Tab=2 space 삽입, Shift+Tab=라인 앞 제거 + requestAnimationFrame 으로 커서 위치 복원. 마크다운 textarea 5곳에 onKeyDown 연결 (MeetingsPage 논의/WbsPage notes/WorkLogPage EditablePreviewField/IssuesPage DescriptionField/DevInfoPage Markdown content)
- [완료] 개선 | 10. 프로젝트 카드 상태/일자 우측 상단 | ProjectList.tsx: 헤더 row 의 우측에 hover 액션버튼 + ProjectStatusBadge + D-N 칩 가로 정렬. 본문 아래 별도 상태/일자 줄 제거 → 카드 세로 1줄 절약. 일자 표시 "N일 남음" → "D-N" 으로 축약
- [완료] 개선 | 11. date/time picker hover 가시성 | index.css: `::-webkit-calendar-picker-indicator` 의 opacity 0.7 + hover 1.0 + cursor pointer 보강. color-scheme: dark 가 색은 자동 처리(이미 :root 에 적용됨)
- [완료] 문서 | CLAUDE.md "마크다운 인플레이스 편집"에 textareaTab 한 줄 추가, "표·카드 안 인라인 상태 변경 (BadgeMenu)" 섹션 신규, "Chromium time picker 의 step 한계" 함정 신규
- [완료] 빌드 | cd frontend; npm run build → tsc + vite 통과 (전 회차 동일)

---

# 2026-05-14
## 메뉴 : 공통(로딩/아이콘/빌드)/회의록/일정WBS/통합모니터링/프로젝트맵/설정
### 요청원문 : 실사용 개선 10개 — 로딩 스피너, 회의록 스크롤·시간 필드, 마크다운 글자크기 슬라이더, 프로젝트맵 타임라인 노드 크기 버그, WBS 인라인 상태 변경, 통합 모니터링 '계획' 제거, 아이콘, single-file 빌드, Atlas.exe 이름

- [완료] 개선 | 1. 초기 로딩 스피너 | MainWindow.xaml: LoadingOverlay 에 Ellipse + RotateTransform Storyboard(StrokeDashArray="35 200" StrokeDashCap=Round, 1.1s 무한 회전). StatusText 카운터 표기 제거 → 정적 "서버를 시작하고 있습니다"
- [완료] 개선 | 2. 회의록 모달 너비/스크롤 | MeetingsPage.tsx MeetingForm: `max-w-7xl` → `max-w-[95vw]`, grid 비율 `md:grid-cols-[minmax(0,1fr)_minmax(0,1.4fr)]` 로 우측 논의 영역 확대. 마크다운 미리보기 div 에 `max-h-[65vh] overflow-y-auto` 추가
- [완료] 신기능 | 3. 회의록 시작/종료 시간 | Meeting.cs: `string? StartTime/EndTime` 추가. MeetingDtos·MeetingService·ProjectService.MeetingToDto 동기화. EF Migration `20260514113318_AddMeetingTimes` 생성. frontend types/Meeting 에 `startTime?`·`endTime?`. MeetingForm grid-cols-12 로 날짜(col-4)+시작/종료 time(col-2 each)+주제(col-4) 한 줄. 카드 메타에 "HH:mm–HH:mm" 표시
- [완료] 신기능 | 4. 마크다운 글자크기·줄간격 슬라이더 | settings.ts: `markdownFontSize`(11–22px, step 0.5)·`markdownLineHeight`(1.2–2.4, step 0.05) 추가. `applyMarkdownStyle` 함수가 `--md-font-size`/`--md-line-height` CSS 변수에 세팅. index.css `.markdown-body` 가 변수 참조. App.tsx 진입 시 theme 와 함께 적용. SettingsPage 에 슬라이더 2개 + 실시간 미리보기(ReactMarkdown 샘플)
- [완료] 버그수정 | 5. 프로젝트맵 타임라인 노드 크기 폭주 | ProjectMapPage.tsx: cytoscape 인스턴스에 mode 별 `maxZoom`(timeline=1.0, 기타=4.0) + `minZoom=0.1`. fit() 결과가 1.0 이상이면 1.0 으로 캡 + boundingBox 좌상단 기준 pan 재배치 → 타임라인에서 노드가 거대하게 보이던 문제 해결
- [완료] 개선 | 6. WBS 인라인 상태 변경 | WbsPage.tsx WbsRow: 상태 셀에 Badge 위에 투명 select 오버레이(`opacity-0 cursor-pointer`). onStatusChange 콜백 → 옵티미스틱 patchStatus(트리 재귀) 후 `wbsApi.update({...rest, status})`, 완료 후 load(). 셀 onClick=stopPropagation 으로 행 dblclick/edit 충돌 방지
- [완료] 개선 | 7. 통합 모니터링 '계획' 필드 제거 | MonitoringPage.tsx FIELD_DEFS 에서 `plan` 제거 — '한 일'·'이슈' 만 노출. MonitoringService.cs 의 카드 필터에서도 Plan 검사 제거 → 계획만 작성된 프로젝트는 카드 자체가 등장하지 않음
- [완료] 신기능 | 8. 앱 아이콘 (지도 모티프) | Resources/atlas.svg: 다크 둥근 사각형 위에 금색 hub + 블루 4개 satellite + 곡선 edge. Resources/build-icon.ps1: PowerShell + System.Drawing 으로 동일 도형을 GDI+ 로 다시 그려 PNG-in-ICO 멀티사이즈(16/20/24/32/40/48/64/96/128/256) 컨테이너 직접 작성 → atlas.ico (20KB). csproj `<ApplicationIcon>` + `<Resource Include>` + MainWindow.xaml `Icon="pack://...atlas.ico"`. frontend/public/favicon.svg 도 동일 모티프로 교체
- [완료] 개선 | 9. DesktopApp single-file 빌드 | Release.pubxml: `PublishSingleFile=true` + `EnableCompressionInSingleFile=true` + `IncludeNativeLibrariesForSelfExtract=true` + `IncludeAllContentForSelfExtract=true`. MainWindow.xaml.cs: single-file 에서 `AppContext.BaseDirectory` 가 임시 추출 폴더라 backend exe 를 `Environment.ProcessPath` 의 디렉토리로 찾도록 수정. WebView2 사용자 데이터 폴더는 `Documents/ProjectManager/WebView2` 로 명시 고정(`CoreWebView2Environment.CreateAsync`)해 세션 캐시 보존
- [완료] 개선 | 10. 실행파일 이름 Atlas.exe | DesktopApp.csproj: `<AssemblyName>Atlas</AssemblyName>` → 빌드 산출물 Atlas.dll/Atlas.exe. publish.ps1 의 검증 경로도 `Atlas.exe` 로 갱신. WebService 어셈블리명/RootNamespace 는 호환성 유지
- [완료] 문서 | CLAUDE.md "Publish 단일파일 분리" 섹션 갱신 — single-file 방식·실행파일명·WebView2 사용자 폴더 명시 메모 반영
- [완료] 빌드 | dotnet build src/ProjectManager.WebService → 경고 0/오류 0
- [완료] 빌드 | dotnet build src/ProjectManager.DesktopApp → 경고 0/오류 0
- [완료] 빌드 | cd frontend; npm run build → tsc + vite 통과

---

# 2026-05-12
## 메뉴 : 공통(테마/타이틀바/프로젝트 전환)/주간 업무일지/통합 모니터링/프로젝트맵
### 요청원문 : 라이트/다크 색 조합 재정의, 작업표시줄 최대화, 프로젝트 드롭다운, 'MiPala' 브랜딩, 주간 업무일지 + WBS/Issue 완료 자동기록 + 통합 모니터링 합산, 프로젝트맵 디자인 개선

- [완료] 개선 | 41. 테마 토큰 시스템 도입 | index.css: CSS custom properties (`--bg-base/sidebar/surface/surface-2/surface-3`, `--text-primary/secondary/muted`, `--border-default/strong`, `--accent`(인디고)/`--accent-2`(앰버) 등)로 단일 진실 소스 구성. 다크/라이트 양쪽 토큰 정의. `.bg-surface`·`.text-primary`·`.border-default` 등 유틸 클래스 + `input/select/textarea` 기본 스타일 통합. 다크 보더 `#2a2a2a`→`#3f3f46` 가시성 ↑
- [완료] 개선 | 42. 레거시 색 클래스 광범위 override | index.css: 컴포넌트 하드코딩 `bg-[#1f1f1f]`/`bg-[#1c1c1c]`/`border-[#2a2a2a]`/`bg-zinc-700~900`/`bg-slate-*`/`text-zinc-*`/`text-slate-*`/`border-zinc-*` 등을 `!important`로 토큰 매핑. 라이트 모드에서 글자색뿐 아니라 배경/보더/스크롤바/마크다운 모두 자연스럽게 전환
- [완료] 개선 | 43. Primary/Nav-active 버튼 패턴 일괄 치환 | 12개 .tsx 파일에서 `bg-zinc-700 hover:bg-zinc-600 text-white` → `bg-indigo-600 hover:bg-indigo-500 text-white`, `bg-zinc-700 text-white`(active 상태) → `bg-indigo-600 text-white`. 라이트 모드에서 흰 텍스트 가독성 확보
- [완료] 신기능 | 44. MiPala 브랜딩 적용 | 사이드바 좌상단 로고: `Mi`(인디고 `#818cf8`) + `Pala`(앰버 `#fbbf24`) 둘 다 bold italic + `MindPalace for Projects` 소제목 (`.logo-mi`/`.logo-pala` CSS 클래스). frontend/index.html title=MiPala. start.ps1/publish.ps1 echo + zip 이름 변경. CLAUDE.md 갱신(자산 폴더/localStorage 키는 호환성 유지 노트 추가)
- [완료] 개선 | 45. WPF 타이틀바 MiPala 적용 | MainWindow.xaml: Title="MiPala", 타이틀바와 로딩 오버레이를 `<Run>` 인라인으로 Mi/Pala 각각 다른 색상·italic·ExtraBold 스타일링. 부제 "MindPalace for Projects" 추가
- [완료] 개선 | 46. WPF 작업표시줄 인식 최대화 | MainWindow.xaml.cs: `OnSourceInitialized` override + `HwndSource.AddHook` 으로 `WM_GETMINMAXINFO` 후킹. `MonitorFromWindow`+`GetMonitorInfo` P/Invoke 로 `rcWork`(작업영역) 사용, `MINMAXINFO.ptMaxSize`/`ptMaxPosition` 을 작업영역 기준으로 설정 → 최대화 시 작업표시줄을 가리지 않음
- [완료] 신기능 | 47. 사이드바 프로젝트 드롭다운 | Layout.tsx: `ProjectSwitcher` 컴포넌트(외부 클릭 닫힘, 현재 프로젝트 라벨, 체크 마크). 현재 sub-route 유지(`/projects/3/wbs` 에서 다른 프로젝트 선택 → `/projects/{newId}/wbs`). 비프로젝트 경로에서는 `/projects/{newId}/dashboard` 로 이동
- [완료] 신기능 | 48. 주간 업무일지 백엔드 | Core/Domain/WorkLog.cs(ProjectId+Date 유니크, Done/Plan/Issues 텍스트). IWorkLogRepository, WorkLogRepository(GetByProjectWeek/GetByProjectDate/GetAllInRange/Upsert). WorkLogDtos.cs(WorkLogDto·UpsertWorkLogDto·WeeklyWorkLogProjectDto·WeeklyWorkLogDto). WorkLogService(StartOfWeek=월요일, GetWeek, Upsert, AppendDoneAsync=중복 라인 skip). WorkLogController(`GET/PUT /api/projects/{id}/worklogs[/{date}]`). AppDbContext+OnModelCreating(unique index). Program.cs DI 등록
- [완료] 신기능 | 49. WBS/Issue 완료 자동 업무일지 기록 | WbsService.UpdateAsync: wasDone(이전) vs Done(현재) 전이 시 `AppendDoneAsync(projectId, Today, "- {name}")` 호출. IssueService.UpdateAsync: `IsCompleted = Resolved||Closed`, not-completed → completed 전이 시 `"- [이슈] {title}"` append. WorkLogService 생성자 주입
- [완료] 신기능 | 50. 통합 모니터링 주간 업무일지 합산 | MonitoringService.GetWeeklyWorkLogsAsync(weekStart): IWorkLogRepository.GetAllInRange + Projects 조인, 프로젝트별 5일치 Done/Plan/Issues 를 요일 라벨(월·화·수·목·금)과 함께 줄 단위로 합침. 빈 프로젝트 제외. MonitoringController `GET /api/monitoring/worklogs/weekly?weekStart=YYYY-MM-DD`
- [완료] 신기능 | 51. EF 마이그레이션 AddWorkLogs | 로컬 dotnet-ef 8.0.16 으로 `20260512131653_AddWorkLogs` 생성. WorkLogs 테이블 + (ProjectId,Date) 유니크 인덱스 + Projects FK Cascade. 앱 시작 시 `MigrateAsync()` 자동 적용
- [완료] 신기능 | 52. 주간 업무일지 프론트엔드 페이지 | pages/WorkLogPage.tsx: 월~금 5컬럼 grid(`lg:grid-cols-5 md:grid-cols-2`), 각 컬럼에 `한 일/계획/이슈` 3개 마크다운 필드. focus=textarea/blur=ReactMarkdown 토글, blur 시 worklogApi.upsert 자동 저장. 주 네비게이션(이전/이번/다음). 오늘 컬럼에 인디고 ring 강조. api/worklog.ts·types/index.ts 추가. App.tsx route + Layout 사이드바 메뉴(`업무일지`, NotebookPen 아이콘)
- [완료] 신기능 | 53. 통합 모니터링 이번/지난 주 섹션 | MonitoringPage.tsx: 기존 "오늘 진행 중 WBS" 아래에 `WeeklySection` 2개(`이번 주 업무일지` + `지난 주 업무일지`) 추가. 프로젝트명 굵게 + 한 일/계획/이슈 세 필드 ReactMarkdown 렌더(빈 필드 라벨 자체 생략). 프로젝트 클릭 → 해당 worklog 페이지로 이동
- [완료] 개선 | 54. 프로젝트맵 디자인 재정의 | ProjectMapPage.tsx: 카테고리별 채도 높은 팔레트(WBS=인디고/Change=오렌지/Meeting=에메랄드/Dev=시안), 허브 노드는 더 굵은 보더+카운트 표시(`WBS\n(12)`), 프로젝트 허브에 인디고 글로우 그림자. 노드 hover 시 absolute-positioned 툴팁(상태/날짜/담당자). 카테고리별 에지 색을 fill과 매치. 툴바에 4개 카테고리 범례 + Maximize2 아이콘. 필터 토글 시 60ms 후 cy.fit 자동 호출. 컨테이너 배경 `radial-gradient(var(--bg-surface) → var(--bg-base))`
- [완료] 빌드 | dotnet build src/ProjectManager.WebService → 경고 0/오류 0
- [완료] 빌드 | dotnet build src/ProjectManager.DesktopApp → 경고 0/오류 0

---

# 2026-05-11 (2)
## 메뉴 : 공통/프로젝트목록/대시보드/일정WBS/변경이력/회의록/개발정보/프로젝트맵/신기능
### 요청원문 : 2026-05-11 검토 결과 요구사항 전체 구현 / 신규 개발건·요구사항 점검은 Opus로 진행

- [완료] 개선 | 20. 타이틀바 다크 회색 재디자인 | MainWindow.xaml: Title="SW 프로젝트 관리", 배경 #141414, 타이틀 텍스트 색 변경
- [완료] 개선 | 21. 짙은 회색 다크 테마 전면 적용 + 라이트 모드 CSS | index.css: body=#141414. Layout.tsx: 사이드바=#1c1c1c, 카드=#1f1f1f, 활성메뉴=bg-zinc-700 (indigo 계열 전면 제거). index.css: html.light 오버라이드(배경/텍스트/border/markdown) 추가
- [완료] 신기능 | 22. 리소스 관리 백엔드 | Core/Domain/Resource.cs(Name·Type:Person|Equipment·Department·Email·Phone·Notes). IRepositories.cs: IResourceRepository 추가. ResourceDtos.cs, ResourceRepository.cs, ResourceService.cs, ResourcesController.cs(GET /api/resources, POST, PUT /{id}, DELETE /{id}, GET /{id}/assignments)
- [완료] 신기능 | 23. 리소스 관리 프론트엔드 | types/index.ts: Resource·ResourceAssignment 타입 추가. api/resources.ts 생성. pages/ResourcesPage.tsx: 목록 CRUD + AssignmentsModal(리소스별 WBS 할당 작업). Layout.tsx: "리소스" 최상단 메뉴 추가. App.tsx: /resources 라우트 추가
- [완료] 신기능 | 24. 이슈 관리 백엔드 | Core/Domain/Issue.cs(Title·Description·Status:Open|InProgress|Resolved|Closed·Priority:High|Medium|Low·AssigneeResourceId FK·DueDate). IIssueRepository 추가. IssueDtos.cs, IssueRepository.cs, IssueService.cs, IssuesController.cs(GET /api/projects/{id}/issues, POST, PUT /{issueId}, DELETE /{issueId})
- [완료] 신기능 | 25. 이슈 관리 프론트엔드 | types/index.ts: Issue 타입 추가. api/issues.ts 생성. pages/IssuesPage.tsx: 이슈 목록 + 등록·수정·삭제 폼(담당자=리소스 select). Layout.tsx projectNavItems에 "이슈관리" 추가(WBS 아래). App.tsx: /projects/:id/issues 라우트 추가
- [완료] 신기능 | 26. 통합 모니터링 화면 | MonitoringService.cs: 전체 프로젝트의 오늘 날짜 포함 InProgress WBS 조회. MonitoringController.cs: GET /api/monitoring/today. types: TodayWbs·MonitoringData 타입. api/monitoring.ts 생성. pages/MonitoringPage.tsx: 프로젝트별 그룹화, 총 건수·프로젝트 수 헤더. Layout.tsx: "통합 모니터링" 최상단 메뉴 추가
- [완료] 신기능 | 27. DB 마이그레이션 | AppDbContext.cs: DbSet<Resource>·DbSet<Issue> 추가, 엔티티 설정(FK·인덱스). EF Migration 20260511130612_AddResourcesAndIssues 생성·적용(Resources·Issues 테이블 신규)
- [완료] 개선 | 28. WBS 우선순위→중요도 select 변경 | WbsPage.tsx: '우선순위' label → '중요도', input[type=number] → select(3=높음/2=중간/1=낮음). importanceLabel·importanceClass 맵 추가. 테이블 헤더 "중요도" 변경
- [완료] 개선 | 29. WBS 등록 폼 2컬럼 + 상세정보 마크다운 | WbsItemForm: max-w-md→max-w-3xl, grid-cols-2 레이아웃. 우측: notes 마크다운 에디터(notesEditing state: focus→textarea, blur→ReactMarkdown 렌더링)
- [완료] 개선 | 30. WBS 담당자 필드 리소스 datalist 연동 | WbsItemForm: resourcesApi.getAll() 호출 → <datalist id="wbs-assignee-list"> 자동완성
- [완료] 개선 | 31. 변경이력 영향도 시각화 개선 (히트맵→bar chart) | ChangeLogsPage.tsx: HeatmapChart → ImpactBarChart(날짜×영향도별 stacked bar, Critical=red/High=orange/Medium=amber/Low=emerald). ECharts bar 타입으로 교체, legend 명시
- [완료] 개선 | 32. 변경이력 회의록 선택 스크롤·키워드 필터 | ChangeLogsPage ChangeLogForm: h-48 overflow-y-auto 컨테이너. meetingKeyword state + filteredMeetings(topic·date 포함 검색) input 추가
- [완료] 개선 | 33. 회의록 참석자 영역 스크롤 + 펼치기 순서 변경 | MeetingsPage MeetingForm 참석자 섹션: max-h-72 overflow-y-auto. MeetingDetail 렌더 순서: 주요 결정사항 → Action Items → 논의 내용
- [완료] 개선 | 34. 회의록 논의 내용 마크다운 토글 | MeetingsPage: discussionEditing state(focus=textarea, blur=ReactMarkdown). MeetingDetail 논의 내용도 ReactMarkdown 렌더링
- [완료] 버그 | 35. 개발정보 이미지 미리보기 수정 | DevInfoPage FilePreview: imageExts 확장자 → <img src=".../preview?t={updatedAt}"> 캐시버스트. onError fallback 메시지 추가
- [완료] 버그 | 36. 개발정보 파일 열기 alert 오류 수정 | DevInfoPage handleOpenFile: fetch() 사용(JSON parse 회피), 응답 ok 여부로만 에러 판별 → 정상 시 alert 없음
- [완료] 개선 | 37. 프로젝트 맵 4방위 레이아웃 + 디자인 개선 | ProjectMapPage.tsx: dagre LR → preset layout. buildPositions: cat-wbs(y -HUB_DIST 위), cat-changes(x +HUB_DIST 우), cat-meetings(y +HUB_DIST 아래), cat-dev(x -HUB_DIST 좌). 각 하위 노드 방위 방향으로 확장. 노드: round-rectangle shape, shadow, Pretendard/Inter 폰트, dark gray 색상
- [완료] 개선 | 38. 대시보드 회의록 참여자 포맷팅 | Dashboard.tsx: utils/meetingHelpers.ts의 attendeesToDisplay 공유 사용. JSON → "소속: 이름1, 이름2 | 소속2: ..." 형태 출력
- [완료] 개선 | 39. 프로젝트 목록 상태·남은일수 전 상태 표시 | ProjectList.tsx: showDaysLeft 조건 InProgress 한정 → endDate 있으면 전 상태 표시(Done 제외). ProjectStatusBadge zinc 팔레트 적용
- [완료] 기타 | 40. Opus 사용 메모리 저장 | memory/feedback_use_opus.md: "신규 개발·요구사항 점검은 claude-opus-4-7 사용" 기록. MEMORY.md 인덱스 업데이트

---

# 2026-05-11 (1)
## 메뉴 : 요구사항 완료 여부 점검
### 요청원문 : SW 프로젝트 관리 앱 요구사항 완료 여부 점검 (2026-05-11)

- [완료] 점검 | A. 윈도우 타이틀바 (최소화/최대화/닫기) | DesktopApp/MainWindow.xaml: WindowStyle="None"+WindowChrome, MinimizeButton·MaximizeButton·CloseButton, IsHitTestVisibleInChrome=True 확인
- [완료] 점검 | B. 짙은 회색 다크 테마 (파란색 제거) | index.css: body=#141414, Layout.tsx: 사이드바=#1c1c1c/#2a2a2a, 모든 페이지 zinc/slate 톤. 파란색 잔존 없음
- [완료] 점검 | C. 라이트 모드 | settings.ts: applyTheme('light'→html.light), index.css: html.light 오버라이드(background/text/border/scrollbar/markdown)
- [완료] 점검 | D. 리소스 관리 (사람/장비) | Core/Domain/Resource.cs(ResourceType.Person|Equipment), ResourcesController, ResourcesPage.tsx
- [완료] 점검 | E. 일정/WBS 담당자 리소스 활용 | WbsPage.tsx: resourcesApi.getAll() → datalist#wbs-assignee-list로 자동완성
- [완료] 점검 | F. 리소스별 할당 작업 확인 | resources/{id}/assignments 엔드포인트, ResourcesPage AssignmentsModal에서 프로젝트별 WBS 목록 출력
- [완료] 점검 | G. 통합 모니터링 (오늘 진행 중) | MonitoringController.cs(today), MonitoringPage.tsx: 프로젝트별 그룹화, 총 건수/프로젝트 수 헤더
- [완료] 점검 | H. 이슈 관리 (리소스 담당자) | Core/Domain/Issue.cs(AssigneeResourceId), IssuesController.cs, IssuesPage.tsx 담당자 select=리소스 목록
- [완료] 점검 | I. 프로젝트 진행 상태 카드 | ProjectList.tsx: ProjectStatusBadge 사용
- [완료] 점검 | J. 프로젝트 남은 일수 카드 | ProjectList.tsx: daysLeft 계산, 초과/임박 색상 분기
- [완료] 점검 | K. 회의록 참여자 JSON 포맷팅 | Dashboard.tsx: attendeesToDisplay(m.attendees) → "소속: 이름1, 이름2" 형태
- [완료] 점검 | L. 우선순위 → 중요도(높음/중간/낮음) | WbsPage.tsx: importanceLabel/importanceClass, select option 3=높음/2=중간/1=낮음, 표 헤더 "중요도"
- [완료] 점검 | M. WBS 등록 폼 우측 단 마크다운 | WbsPage WbsItemForm: grid-cols-2, 우측 notes 마크다운 영역
- [완료] 점검 | N. 마크다운 포커스 토글 | WbsPage notesEditing 상태, MeetingsPage discussionEditing 상태(focus=textarea, blur=ReactMarkdown)
- [완료] 점검 | O. 영향도 시각화 개선 (히트맵→stacked bar) | ChangeLogsPage.tsx ImpactBarChart: 날짜별 영향도별 stacked bar, 색상 변경(Critical=red, High=orange, Medium=amber, Low=emerald)
- [완료] 점검 | P. 회의록 선택 스크롤 (5개 초과) | ChangeLogsPage: h-48 overflow-y-auto 컨테이너
- [완료] 점검 | Q. 회의록 키워드 필터링 | ChangeLogsPage: meetingKeyword state, filteredMeetings (주제·날짜 검색)
- [완료] 점검 | R. 참석자 칸 스크롤 | MeetingsPage: max-h-72 overflow-y-auto
- [완료] 점검 | S. 펼치기 순서 (결정사항→AI→논의) | MeetingsPage MeetingDetail: 1.주요 결정사항 → 2.Action Items → 3.논의 내용 순
- [완료] 점검 | T. 논의 내용 마크다운 토글 | MeetingsPage: discussionEditing, ReactMarkdown 렌더링
- [완료] 점검 | U. 파일 이미지 미리보기 | DevInfoPage FilePreview: imageExts 확장자→<img>, 캐시버스트 쿼리
- [완료] 점검 | V. 파일 열기 alert 오류 수정 | DevInfoPage handleOpenFile: fetch 사용으로 JSON parse 회피, 에러 메시지만 alert
- [완료] 점검 | W. 프로젝트 맵 디자인 | ProjectMapPage: Pretendard/Inter 폰트, round-rectangle shape, shadow, kind별 색상 구분
- [완료] 점검 | X. 4방위 레이아웃 (↑WBS, →변경이력, ↓회의록, ←개발정보) | ProjectMapPage buildPositions: cat-wbs(y -HUB_DIST), cat-changes(x +HUB_DIST), cat-meetings(y +HUB_DIST), cat-dev(x -HUB_DIST). preset layout
- [완료] 빌드 | dotnet 빌드 성공 (경고 0/오류 0) | dotnet build src/ProjectManager.WebService 통과
- [완료] 빌드 | 프론트엔드 빌드 성공 | npm run build → dist/index.html, assets/index-CqRqrZmJ.css, index-DpqVKDLr.js
- [완료] 배포 | wwwroot 동기화 | frontend/dist/* → src/ProjectManager.WebService/wwwroot/ 복사

---

# 2026-05-10 (3)
## 메뉴 : 배포 설정
### 요청원문 : 배포 하려면

- [완료] 신기능 | 17. WebService publish 시 React 프론트엔드 자동 빌드 타겟 추가 | ProjectManager.WebService.csproj: `PublishFrontend` MSBuild 타겟(BeforeTargets="BeforePublish") 추가, `npm run build` 실행 후 `frontend/dist/**` → `wwwroot/` 복사
- [완료] 신기능 | 18. WebService Release publish 프로필 생성 | Properties/PublishProfiles/Release.pubxml: win-x64, SelfContained=true, PublishSingleFile=true, EnableCompressionInSingleFile=true, 출력경로=`../../../../publish/`
- [완료] 신기능 | 19. DesktopApp Release publish 프로필 생성 | Properties/PublishProfiles/Release.pubxml: win-x64, SelfContained=true, PublishSingleFile=false(WebView2 단일파일 패키징 충돌 방지), 출력경로=`../../../../publish/`

---

# 2026-05-10 (2)
## 메뉴 : UI/UX 전반 개선
### 요청원문 : 2026-05-10 검토 결과 (UI/UX 종합 리뷰)

- [완료] 개선 | 11. 전체 아이콘 lucide-react로 교체 및 컬러 테마 정리 | Layout.tsx: 사이드바 아이콘 전체 lucide-react 적용. index.css: .markdown-body 스타일 추가(h1-h6, code, pre, blockquote, table 등)
- [완료] 신기능 | 12. 프로젝트 상태(ProjectStatus) 추가 | Core/Domain/Project.cs: `ProjectStatus` enum(Planned/Waiting/InProgress/Done) 및 `Status` 프로퍼티 추가. Core/DTOs/ProjectDtos.cs: 모든 DTO에 Status 포함. ProjectList.tsx: 상태 배지·days remaining 표시, Dashboard.tsx: 헤더 상태 배지
- [완료] 신기능 | 13. 회의록 attendees/decisions/actionItems 구조화 | MeetingsPage.tsx: `AttendeeOrg[]`·`string[]`·`{content,assignee,deadline}[]` 타입 정의, 기존 TEXT 컬럼에 JSON 직렬화 저장, parseAttendees/parseDecisions/parseActionItems 레거시 폴백 처리. 2열 폼(좌: 메타+참석자+결정사항+액션아이템, 우: 토의내용)
- [완료] 개선 | 14. WBS 테이블·Gantt 개선 | WbsPage.tsx: InProgress=text-amber-400, Done=text-emerald-400, ChevronDown/Right expand-collapse, 행 더블클릭→편집 모달, Gantt 시작일순 정렬·최상위=#6366f1·자식=#818cf8·더블클릭→DateEditModal
- [완료] 개선 | 15. DevInfo ReactMarkdown 렌더링 + 파일 프리뷰 | DevInfoPage.tsx: `react-markdown` + `className="markdown-body"` 적용. File 타입: 숨김 `<input type="file">` → `/devinfo/upload` → filePath 저장. 이미지는 `<img>` 렌더, 텍스트는 `<pre>` 렌더(`/preview` 엔드포인트 이용)
- [완료] 개선 | 16. WPF 커스텀 크롬 구현 | MainWindow.xaml: `WindowStyle="None"` + `WindowChrome`(CaptionHeight=40, ResizeBorderThickness=6), 인디고 도트·"PM Hub" 타이틀, 최소화/최대화/닫기 버튼(IsHitTestVisibleInChrome=True). App.xaml: ChromeButtonStyle·ChromeCloseButtonStyle 정의

---

# 2026-05-10 (1)
## 메뉴 : 전체 앱 초기 개발
### 요청원문 : docs/개인용_로컬_프로젝트_관리_앱_설계.pdf 를 읽어서 앱 개발해줘 / .net core 8.0 으로 해야돼

- [완료] 신기능 | 1. PDF 설계 문서 분석 및 아키텍처 설계 | "Obsidian + Gantt + Node graph + Timeline" 컨셉. C# ASP.NET Core 8 백엔드 + SQLite, WPF+WebView2 데스크탑 셸, React+TypeScript 프론트엔드 5레이어 구조 확정
- [완료] 신기능 | 2. .NET 8 Clean Architecture 솔루션 구성 | Core / Infrastructure / Application / WebService / DesktopApp 5개 프로젝트. EF Core 8.0.16 + SQLite. 글로벌 dotnet-ef v10 충돌 → 로컬 tool-manifest에 v8.0.16 설치(`dotnet new tool-manifest`)
- [완료] 신기능 | 3. 도메인 모델 6개 정의 | Core/Domain: Project(ProjectStatus enum), WbsItem+WbsVersion(WbsStatus enum), ChangeLog(ImpactLevel enum), DevInfoItem(DevInfoType enum), Meeting. AppDbContext cascade delete 설정
- [완료] 신기능 | 4. Repository + Service 구현 | IRepositories.cs 인터페이스 5종. Infrastructure: ProjectRepository, WbsRepository, ChangeLogRepository, MeetingRepository, DevInfoRepository. Application: ProjectService(백업 zip 생성·FileShare.ReadWrite\|Delete), DevInfoService(IProjectRepository 주입→project.FolderPath에 .md 저장)
- [완료] 신기능 | 5. ASP.NET Core Web API Controllers 구현 | Program.cs: `AddControllers().AddJsonOptions(JsonStringEnumConverter)`, `UseDefaultFiles`, `UseStaticFiles`, `MapFallbackToFile("index.html")`. Controllers: Projects(dashboard·backup 포함), Wbs, WbsVersions, ChangeLogs, Meetings, DevInfo(upload·preview·open), Health
- [완료] 신기능 | 6. PathResolver + DevFilesStorage 구현 | PathResolver: 기본경로=`Documents/ProjectManager/`, 빈 프로젝트명 → "Project" 폴백. DevFilesStorage: SaveFileAsync, OpenFile(OS shell execute), DeleteFile
- [완료] 신기능 | 7. React + TypeScript + Vite + TailwindCSS 프론트엔드 초기 구성 | frontend/: Vite 프로젝트, @tailwindcss/vite, Zustand, echarts-for-react, cytoscape+cytoscape-dagre, lucide-react, react-markdown. vite.config.ts: `/api` → `http://localhost:5200` 프록시
- [완료] 신기능 | 8. 전체 페이지 구현 | ProjectList(2열폼·백업다운로드), Dashboard(섹션 클릭 이동·ProjectStatus 배지), WbsPage(트리테이블·Gantt), ChangeLogsPage(히트맵·회의록 연결), MeetingsPage, DevInfoPage, ProjectMapPage(Cytoscape dagre LR), SettingsPage(localStorage)
- [완료] 신기능 | 9. Zustand 상태관리 + API 레이어 | useProjectStore.ts: projects·selectedProjectId. api/: client.ts(base fetch), projects.ts(backup blob 다운로드), wbs.ts, changelogs.ts, meetings.ts, devinfo.ts. Layout.tsx: 마지막 선택 프로젝트 localStorage 자동 복원
- [완료] 신기능 | 10. WPF + WebView2 데스크탑 앱 | MainWindow.xaml.cs: StartBackend()(AppContext.BaseDirectory에서 WebService.exe 실행), WaitForBackendAsync()(/api/health 40회×500ms 폴링), WebView2 `http://localhost:5200` 이동, OnClosing() Kill(entireProcessTree=true)
- [완료] 버그 | 11. enum 직렬화 오류 수정 | Minimal API의 ConfigureHttpJsonOptions가 Controller에 미적용 → `AddControllers().AddJsonOptions(JsonStringEnumConverter)`로 전환
- [완료] 버그 | 12. EF 마이그레이션 도구 버전 충돌 수정 | 글로벌 dotnet-ef v10이 net8.0 프로젝트 인식 불가 → 로컬 tool-manifest v8.0.16 설치, 기존 마이그레이션 삭제 후 재생성
- [완료] 버그 | 13. SQLite 파일 락 백업 오류 수정 | `File.OpenRead` 실패 → `FileShare.ReadWrite | FileShare.Delete` 모드로 변경(ProjectService.cs: CreateBackupAsync)
- [완료] 버그 | 14. 백엔드 포트 충돌(5050) 수정 | 포트 5050 사용 중 → 5200으로 변경. appsettings.json Urls, vite.config.ts proxy 대상 동시 수정
