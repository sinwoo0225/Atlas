# Build artifacts
**/bin/
**/obj/
**/publish/
**/dist/
**/node_modules/
**/.vite/

# WebService static files — generated by `npm run build` (frontend) and copied at publish.
# Source lives in frontend/public/ and frontend/src/.
src/ProjectManager.WebService/wwwroot/

# IDE / editor
.vs/
.vscode/
.idea/
*.user
*.suo
*.sln.docstates

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local databases / packages
*.db
*.db-shm
*.db-wal

# OS junk
.DS_Store
Thumbs.db

# Archive
*.zip

# Release artifacts (publish.ps1 -Installer 가 레포 루트에 생성)
Atlas-Setup-*.exe
# MSIX 패키지 (publish.ps1 -Msix) 와 로컬 테스트 자체서명 인증서
Atlas-*.msix
*.cer

# Misc
publish/

# Claude Code (machine-local settings + working context)
.claude/
CLAUDE.md

# Local-only dev notes
docs/

# Promo video (Remotion) — local only, not part of the app/repo
video/
