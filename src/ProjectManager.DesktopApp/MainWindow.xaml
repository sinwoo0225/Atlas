<Window x:Class="ProjectManager.DesktopApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="SW 프로젝트 관리" Height="900" Width="1400"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="#141414">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40" ResizeBorderThickness="6"
                            UseAeroCaptionButtons="False" GlassFrameThickness="0"/>
    </shell:WindowChrome.WindowChrome>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom title bar -->
        <Border Grid.Row="0" Background="#1c1c1c" BorderBrush="#2a2a2a" BorderThickness="0,0,0,1" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0">
                    <Rectangle Width="14" Height="14" Fill="#6b7280" RadiusX="3" RadiusY="3" Margin="0,0,10,0"/>
                    <TextBlock Text="SW 프로젝트 관리" Foreground="#e5e7eb" FontSize="13"
                               FontWeight="SemiBold" FontFamily="Segoe UI"
                               VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                            VerticalAlignment="Center" Margin="0,0,4,0">
                    <Button x:Name="MinimizeButton" Content="&#x2500;" Width="40" Height="40"
                            Style="{StaticResource ChromeButtonStyle}"
                            Click="MinimizeButton_Click"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
                    <Button x:Name="MaximizeButton" Content="&#x25A1;" Width="40" Height="40"
                            Style="{StaticResource ChromeButtonStyle}"
                            Click="MaximizeButton_Click"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
                    <Button x:Name="CloseButton" Content="&#x2715;" Width="40" Height="40"
                            Style="{StaticResource ChromeCloseButtonStyle}"
                            Click="CloseButton_Click"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- WebView2 -->
        <wv2:WebView2 x:Name="WebView" Grid.Row="1"/>

        <!-- Loading overlay -->
        <Border x:Name="LoadingOverlay" Grid.Row="1" Background="#141414">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="320">
                <TextBlock Text="SW 프로젝트 관리" Foreground="#e5e7eb" FontSize="28"
                           FontWeight="Bold" HorizontalAlignment="Center"
                           FontFamily="Segoe UI"/>
                <TextBlock x:Name="StatusText" Text="서버 시작 중..."
                           Foreground="#9ca3af" FontSize="13"
                           HorizontalAlignment="Center" Margin="0,12,0,0"
                           FontFamily="Segoe UI"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
